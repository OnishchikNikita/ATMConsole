package com.senla.server.controller.request;

public class CardBlockRequest extends Request {
    public CardBlockRequest(RequestType type, String cardNumber) {
        super(type, cardNumber);
    }
}
